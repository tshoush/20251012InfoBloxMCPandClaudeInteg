# InfoBlox Configuration
# Replace these values with your actual InfoBlox credentials

# InfoBlox Grid hostname or IP address
INFOBLOX_HOST=your-infoblox-host.example.com

# InfoBlox username (admin or service account)
INFOBLOX_USER=your-username

# InfoBlox password
INFOBLOX_PASSWORD=your-password

# WAPI version (default: v2.13.1)
WAPI_VERSION=v2.13.1

# SSL Certificate Verification
# Set to "true" to verify SSL certificates (RECOMMENDED)
# Set to "false" ONLY in isolated lab environments (INSECURE)
INFOBLOX_VERIFY_SSL=true

# Optional: Path to custom CA bundle if using self-signed certificates
# INFOBLOX_CA_BUNDLE=/path/to/infoblox-ca.pem

# Anthropic API Configuration
# Get your API key from: https://console.anthropic.com/

# Anthropic API key for Claude
ANTHROPIC_API_KEY=your-anthropic-api-key

# RAG Configuration (Optional)
# RAG_DB_PATH=~/.infoblox-rag
# RAG_COLLECTION_NAME=infoblox_knowledge

# Logging Configuration (Optional)
# LOG_LEVEL=INFO
# LOG_FILE=ddi-assistant.log
# ENABLE_SECURITY_AUDIT=true
